# 野指针

- 现象：Memory access error: write outside mapped memory
    - 某个指针指向内容后面出现乱码
- 发现该指针没有初始化，没有分匹配内存。

-解决：初始化指针地址，绑定一块内存

二级指针未初始化，导致不可预见的行文，跑飞了。
绑定一级指针。

const 赋值操作。

字面量被更改了 宏定义被更改了。

